# 🚀 БЫСТРЫЙ СТАРТ

## Что вы получили?

Полностью функциональный статический сайт с ретро CRT эффектами, глитч-анимациями и полноценным функционалом e-commerce.

### ✨ Основные функции

- ✅ **Главная страница** с splash screen и TV-помехами
- ✅ **Каталог товаров** с вертикальной прокруткой
- ✅ **Страница товара** с медиа-галереей
- ✅ **Корзина** с LocalStorage (данные сохраняются)
- ✅ **Оформление заказа** с формой и выбором доставки
- ✅ **Адаптивность** для всех устройств
- ✅ **CRT эффекты**: вибрация, bloom, хроматика, скан-линии
- ✅ **Глитч-анимации**: переходы, ломаные экраны, TV-помехи

---

## 📦 Локальное тестирование

### Вариант 1: Python (рекомендуется)

Windows:
```bash
run-local-server.bat
```

Linux/Mac:
```bash
chmod +x run-local-server.sh
./run-local-server.sh
```

### Вариант 2: Node.js (http-server)

```bash
npm install -g http-server
http-server
```

### Вариант 3: VS Code Live Server

- Установите расширение "Live Server"
- Кликните правой кнопкой на `index.html`
- "Open with Live Server"

**Откройте браузер:** http://localhost:8000

---

## 🎨 Структура файлов

```
Szhima-site/
├── index.html                 # Главная (с логотипом и 2 кнопками)
├── catalog.html               # Каталог (прокрутка товаров)
├── product.html               # Товар (галерея + корзина)
├── order.html                 # Заказ (форма оформления)
│
├── data/
│   └── products.json          # ЗДЕСЬ ДОБАВЛЯЙТЕ ТОВАРЫ ↓
│
├── assets/
│   ├── css/
│   │   ├── global.css         # Глобальные стили + CRT
│   │   ├── index.css          # Главная страница
│   │   ├── catalog.css        # Каталог
│   │   ├── product.css        # Товар
│   │   └── order.css          # Заказ
│   │
│   ├── js/
│   │   ├── utils.js           # Функции для корзины
│   │   ├── home.js            # Главная логика
│   │   ├── catalog.js         # Каталог логика
│   │   ├── product.js         # Товар логика
│   │   └── order.js           # Заказ логика
│   │
│   └── media/
│       ├── placeholder.svg    # Заглушка для товаров
│       └── logo.svg           # SVG логотип
│
└── effects/
    ├── noise.js               # Генерация шума
    ├── crt.js                 # CRT эффекты
    └── glitch.js              # Глитч-анимации
```

---

## 📝 Добавление товаров

### Шаг 1: Откройте `/data/products.json`

### Шаг 2: Добавьте товар в массив "products"

```json
{
  "id": "unique-id",
  "title": "Название товара",
  "article": "ART-001",
  "price": 2999,
  "category": "clothing",
  "sizes": ["S", "M", "L", "XL"],
  "media": [
    { "type": "image", "src": "/assets/media/img1.jpg" },
    { "type": "image", "src": "/assets/media/img2.jpg" },
    { "type": "video", "src": "/assets/media/video.mp4" }
  ]
}
```

### Шаг 3: Добавьте изображения

- Сохраните ваши изображения в `/assets/media/`
- Используйте пути в JSON (например: `/assets/media/my-product.jpg`)

### Поддерживаемые форматы

- **Изображения**: JPG, PNG, GIF, WebP, SVG
- **Видео**: MP4, WebM, OGG

---

## 🎨 Настройка дизайна

### Изменить цвета

Откройте `/assets/css/global.css` и отредактируйте переменные:

```css
:root {
  --primary-color: #00ffff;     /* Основной цвет (голубой) */
  --secondary-color: #ff00ff;   /* Вторичный цвет (розовый) */
  --accent-color: #ffff00;      /* Акцентный цвет (жёлтый) */
  --bg-dark: #1a1a1a;           /* Светлый фон */
  --bg-darker: #0a0a0a;         /* Тёмный фон */
}
```

### Изменить логотип

Вариант 1: Отредактируйте символ в HTML
```html
<div class="logo">◆</div>  <!-- Измените на ✦, ◈, ⬠ и т.д. -->
```

Вариант 2: Используйте изображение
```html
<div class="logo">
  <img src="/assets/media/my-logo.png" alt="Logo">
</div>
```

### Изменить шрифт

В `global.css`:
```css
@import url('https://fonts.googleapis.com/css2?family=YOUR_FONT:wght@400;700;900&display=swap');

body {
  font-family: 'YOUR_FONT', monospace;
}
```

---

## 🌍 Развёртывание на GitHub Pages

### Шаг 1: Создайте репозиторий

```bash
git init
git add .
git commit -m "Initial commit: SZHIMA store"
```

### Шаг 2: Загрузите на GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/Szhima-site.git
git branch -M main
git push -u origin main
```

### Шаг 3: Включите Pages

1. Settings → Pages
2. Branch: `main`, Folder: `/ (root)`
3. Save

**Сайт будет доступен:** `https://YOUR_USERNAME.github.io/Szhima-site/`

> 📖 Подробнее в `DEPLOYMENT.md`

---

## 🎮 Как работает?

### Главная страница
1. Загружается **splash screen** с TV-помехами (2.5 сек)
2. Показывается логотип с **CRT эффектом**
3. Две кнопки:
   - "Каталог" → переходит в `/catalog.html`
   - "Не нажимай меня" → открывает 5 вкладок + краш-эффект

### Каталог
1. Загружает товары из `/data/products.json`
2. **Скролл мышью** или **свайп на мобильных** = переход между товарами
3. Каждый товар показывается с **плавной глитч-анимацией**
4. Клик "Подробнее" → `/product.html?id=ТОВАРА`

### Товар
1. Показывается информация (название, цена, артикул)
2. Медиа-галерея с миниатюрами
3. Для одежды: выбор размера
4. Кнопка "Добавить в корзину"
5. Товар сохраняется в **LocalStorage** (навсегда!)

### Корзина
1. Список всех товаров из `localStorage`
2. Можно удалить товары
3. Форма оформления (ФИО, адрес, доставка, оплата)
4. Промокод "GLITCH" дает 10% скидку
5. Кнопка "Заказать" → краш-эффект + очистка корзины

---

## 💾 LocalStorage

Корзина автоматически сохраняется в браузер. Товары остаются даже после закрытия окна.

Чтобы очистить корзину вручную:
```javascript
// В консоли браузера (F12)
localStorage.removeItem('szhima_cart');
```

---

## 🎯 Полезные советы

### Добавить видео вместо картинок

В `products.json`:
```json
"media": [
  { "type": "video", "src": "/assets/media/product-video.mp4" },
  { "type": "image", "src": "/assets/media/product-cover.jpg" }
]
```

### Скрыть выбор размера

Если товар не одежда (категория ≠ "clothing"), размер автоматически скроется.

### Изменить эффекты

Отредактируйте файлы в `/effects/`:
- `crt.js` - CRT эффекты (вибрация, bloom и т.д.)
- `glitch.js` - Глитч-анимации
- `noise.js` - Генерация шума

### Выключить эффекты

В `assets/js/home.js`, `catalog.js` и т.д. закомментируйте строки типа:
```javascript
// crt.createScanlines(document.body);
// crt.addBloom(logo);
```

---

## 🐛 Проблемы?

### Белая страница при загрузке

1. Откройте консоль (F12)
2. Проверьте ошибки
3. Убедитесь, что пути правильные (начинаются с `/`)

### JSON не загружается

Проверьте:
- Путь в `utils.js` → `fetch('/data/products.json')`
- Файл существует в `/data/products.json`

### Товары не отображаются

1. Проверьте `products.json` валидность (используйте JSONLint)
2. Убедитесь, что у каждого товара есть `id` и `media`
3. Откройте консоль (F12) → Network → посмотрите на ошибки

### Стили не применяются

- Ctrl+Shift+Delete (очистить кеш) и обновить страницу
- Или Ctrl+Shift+R (hard refresh)

---

## 📱 Тестирование на мобильных

### DevTools (Chrome/Firefox)

1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Выберите устройство (iPhone, Android и т.д.)
3. Проверьте адаптивность

### На реальном устройстве

1. Запустите локальный сервер
2. Узнайте ваш IP: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
3. На телефоне откройте: `http://YOUR_IP:8000`

---

## 📚 Дополнительные ресурсы

- `README.md` - Полная документация проекта
- `DEPLOYMENT.md` - Развёртывание на GitHub Pages
- `package.json` - Информация о проекте

---

## ✅ Готово!

Ваш сайт полностью функционален и готов к использованию.

### Далее вы можете:

1. ✏️ **Добавить товары** в `products.json`
2. 🖼️ **Добавить изображения** в `/assets/media/`
3. 🎨 **Настроить цвета** в `global.css`
4. 🌍 **Развернуть на GitHub Pages**
5. 🔗 **Подключить пользовательский домен**

---

## 🎉 Удачи!

Наслаждайтесь вашим ретро CRT глитч-сайтом! 📺✨

Если возникнут вопросы, проверьте документацию или сгенерируйте новый репозиторий.
