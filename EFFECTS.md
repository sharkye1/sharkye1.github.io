# 🎨 ПОЛНЫЙ СПИСОК ЭФФЕКТОВ И АНИМАЦИЙ

## CRT МОНИТОР ЭФФЕКТЫ

### 1. **Скан-линии (Scanlines)**
- Горизонтальные полосы, как на старом ТВ
- Применяется ко всему сайту через `::before` псевдоэлемент body
- Цвет: `rgba(0, 0, 0, 0.15)` с высотой 2px

```css
background-image: repeating-linear-gradient(
  0deg,
  rgba(0, 0, 0, 0.15) 0px,
  rgba(0, 0, 0, 0.15) 1px,
  transparent 1px,
  transparent 2px
);
```

### 2. **Хроматическая аберрация (Chromatic Aberration)**
- Разделение цветов (красный, голубой сдвиг)
- Создаёт эффект "волмозящейся" графики
- Используется на логотипе и кнопках

```css
filter: drop-shadow(2px 0 0 #FF00FF) drop-shadow(-2px 0 0 #00FFFF)
```

### 3. **Вибрация (Vibration/Jitter)**
- Логотип вибрирует на месте каждые 0.3 сек
- Трансформация по X и Y с небольшим поворотом
- Создаёт ощущение нестабильности экрана

```css
@keyframes vibrate {
  0% { transform: translate(0, 0) rotate(0deg); }
  25% { transform: translate(4px, -4px) rotate(1.5deg); }
  50% { transform: translate(-4px, 4px) rotate(-1.5deg); }
  75% { transform: translate(4px, 4px) rotate(1deg); }
}
```

### 4. **Bloom (Свечение)**
- Яркое свечение вокруг логотипа
- Несколько слоёв drop-shadow для объёма

```css
filter: drop-shadow(0 0 30px rgba(0, 255, 255, 0.6))
        drop-shadow(0 0 60px rgba(255, 0, 150, 0.4));
```

### 5. **CRT Curve (Кривизна экрана)**
- Радиальный градиент, затемняющий края
- Создаёт впечатление выпуклого экрана

```css
background: radial-gradient(ellipse at center, transparent 0%, rgba(0, 0, 0, 0.3) 100%);
```

### 6. **Шум (Noise)**
- SVG фильтр feTurbulence с fractalNoise
- Добавляет текстуру старого ТВ фона

```html
<feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4"/>
```

---

## ГЛИТЧ-АНИМАЦИИ

### 1. **Glitch Transition (Переход с глитчем)**
- Используется при смене товаров в каталоге
- Элемент раскалывается на части с хроматическими сдвигами
- Длительность: 300-400ms

```javascript
@keyframes glitch-transition {
  0% { opacity: 1; transform: translate(0, 0) skew(0deg); }
  20% { opacity: 0.9; transform: translate(-5px, 3px) skew(2deg); }
  40% { opacity: 0.7; transform: translate(4px, -3px) skew(-1deg); }
  60% { opacity: 0.5; transform: translate(-3px, 2px) skew(1deg); }
  80% { opacity: 0.3; transform: translate(2px, -2px) skew(-0.5deg); }
  100% { opacity: 0; transform: translate(0, 0) skew(0deg); }
}
```

### 2. **TV Static Flash (Вспышка помех)**
- Белая вспышка как статика на ТВ
- Используется при добавлении товара в корзину
- Длительность: 150-200ms

```javascript
animation: staticFlash 200ms ease-out forwards;
```

### 3. **TV Noise (Динамичные помехи)**
- Движущиеся горизонтальные полосы
- На главной странице (splash screen) и при крахе
- Эмулирует сбой сигнала

```css
background-size: 100% 8px;
animation: tvNoise 0.1s infinite;
```

### 4. **Crash Screen (Ломаный экран)**
- Кратковременная потеря сигнала
- Используется при клике "Не нажимай меня" и заказе
- Комбинация градиентов и фильтров

```javascript
animation: crash 400ms ease-out forwards;
```

### 5. **Distort (Искажение)**
- `clip-path` анимация, создающая рваные края
- Эффект неправильного отрисовки пикселей

```css
@keyframes distort {
  0% { clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%); }
  25% { clip-path: polygon(0% 5%, 100% 0%, 100% 95%, 0% 100%); }
  50% { clip-path: polygon(5% 0%, 100% 3%, 95% 100%, 0% 97%); }
  75% { clip-path: polygon(0% 2%, 98% 0%, 100% 98%, 2% 100%); }
  100% { clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%); }
}
```

### 6. **Dissolve (Растворение)**
- Элемент исчезает с лёгким сдвигом
- Используется при удалении товаров из корзины

```css
@keyframes dissolve {
  0% { opacity: 1; transform: translate(0, 0); }
  50% { opacity: 0.5; transform: translate(5px, -5px); }
  100% { opacity: 0; transform: translate(10px, 10px) scale(0.9); }
}
```

---

## SPLASH SCREEN

### 1. **Logo Vibration**
- Логотип на splash screen вибрирует
- Чаще, чем основной логотип (каждые 0.3 сек)

### 2. **Logo Glow**
- Свечение меняется каждые 2 секунды
- Ярче на 50% временной шкалы

### 3. **Text Glitch**
- Текст "LOADING..." мерцает с хроматическими сдвигами
- Каждые 0.4 сек меняются цвета теней

### 4. **Vertical Jitter**
- Логотип прыгает вверх-вниз на 2px
- Создаёт эффект нестабильности видеосигнала

---

## КАТАЛОГ

### 1. **Product Enter Animation**
- Товар появляется снизу с поворотом перспективы
- Трансформация: `translateY(100%) rotateX(45deg)` → `translateY(0) rotateX(0deg)`
- Легкое затемнение переходит в нормальную яркость

```css
@keyframes productEnter {
  0% { opacity: 0; transform: translateY(100%) rotateX(45deg); filter: brightness(0.5); }
  50% { opacity: 0.7; transform: translateY(10px) rotateX(10deg); filter: brightness(0.8); }
  100% { opacity: 1; transform: translateY(0) rotateX(0deg); filter: brightness(1); }
}
```

### 2. **Product Exit Animation**
- Товар исчезает вверх с поворотом
- Трансформация: `translateY(0)` → `translateY(-100%) rotateX(-60deg)`

### 3. **Button Shine Effect**
- Белый градиент скользит по кнопке при наведении
- Эффект как блик на глянце

---

## ГЛАВНАЯ СТРАНИЦА

### 1. **Splash Fade Out**
- Экран загрузки исчезает с масштабированием
- Сначала уменьшается контраст, потом уменьшается масштаб и прозрачность

```css
@keyframes splashFadeOut {
  0% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(1.05); filter: brightness(0.3); }
  100% { opacity: 0; transform: scale(0.95); }
}
```

### 2. **Home Page Fade In**
- Главная страница появляется с глитчем
- Использует класс `fadeInGlitch`

### 3. **Logo CRT Effects Combined**
- На логотипе комбинируются все эффекты одновременно:
  - Вибрация
  - Свечение (glow)
  - Хроматическая аберрация (на hover)
  - Bloom (на hover)

---

## КНОПКИ

### 1. **Button Hover State**
- Фон меняется с gradient
- Появляется блик (shine effect)
- Кнопка слегка сдвигается и масштабируется
- Добавляется drop-shadow с хроматикой

```css
.home-button:hover {
  transform: skew(-3deg) scale(1.1);
  filter: drop-shadow(3px 0 0 var(--secondary-color)) 
          drop-shadow(-3px 0 0 var(--primary-color));
}
```

### 2. **Danger Button Pulse**
- Красная кнопка "Не нажимай меня" пульсирует
- Свечение меняется каждые 1.5 сек

```css
@keyframes dangerPulse {
  0%, 100% { box-shadow: 0 0 10px #ff0000; }
  50% { box-shadow: 0 0 25px #ff0000; }
}
```

### 3. **Button Active (Pressed)**
- При клике кнопка сжимается (scale 0.95)
- Увеличивается внутреннее свечение

---

## ТОВАР

### 1. **Media Enter**
- Изображение появляется с масштабированием (0.95 → 1)
- Прозрачность переходит от 0 к 1

### 2. **Media Thumb Hover**
- Миниатюра становится ярче и светится
- Трансформируется в активное состояние

---

## КОРЗИНА

### 1. **Item Remove Animation**
- Товар растворяется при удалении (dissolve)
- Красивое исчезновение с небольшим сдвигом

### 2. **Empty Cart Message**
- Если корзина пуста, показывается сообщение с гнёздом

---

## ЦВЕТОВАЯ ПАЛИТРА

```css
--primary-color: #00ffff      /* Голубой (основной) */
--secondary-color: #ff00ff    /* Розовый (вторичный) */
--accent-color: #ffff00       /* Жёлтый (акцент) */
--bg-dark: #1a1a1a           /* Серый фон */
--bg-darker: #0a0a0a         /* Чёрный фон */
```

---

## ТЕКСТОВЫЕ ЭФФЕКТЫ

### 1. **Text Shadow (Теневой текст)**
- Используется трёхслойное drop-shadow
- Создаёт эффект рассеивания света по RGB каналам

```css
text-shadow: 
  0 0 10px var(--primary-color),
  2px 2px 0px var(--secondary-color),
  -2px -2px 0px var(--primary-color);
```

### 2. **Glitch Text**
- Текст мерцает с подменой символов на кириллицу
- 50ms интервал между обновлениями

---

## ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Performance
- Все анимации используют `transform` и `opacity` для оптимизации GPU
- Сканлинии используют `background-image` вместо canvas
- Шум генерируется один раз через SVG фильтр

### Browser Support
- Chrome/Edge: 100% поддержка
- Firefox: 100% поддержка
- Safari: 95% (некоторые фильтры могут работать медленнее)
- IE: ❌ Не поддерживается

### Отключение эффектов
Чтобы выключить эффекты, закомментируйте в JS:
```javascript
// crt.createScanlines(document.body);
// glitch.createTVNoise();
// crt.applyChromatic(element);
// crt.addBloom(element);
// crt.addVibration(element);
```

---

## Примеры использования

### Добавить эффект к элементу
```javascript
import { CRTEffect } from './effects/crt.js';
const crt = new CRTEffect();

const element = document.querySelector('.my-element');
crt.applyChromatic(element);      // Хроматика
crt.addBloom(element);             // Свечение
crt.addVibration(element, 3000, 2); // Вибрация
```

### Запустить глитч анимацию
```javascript
import { GlitchEffect } from './effects/glitch.js';
const glitch = new GlitchEffect();

await glitch.glitchTransition(element, 300);
glitch.tvStaticFlash(200);
await glitch.crashScreen(400);
```

---

## Кастомизация

Отредактируйте параметры в файлах:
- `effects/crt.js` - интенсивность вибрации, размер bloom и т.д.
- `effects/glitch.js` - длительность анимаций, интенсивность эффектов
- `effects/noise.js` - частота и детализация шума
- `assets/css/global.css` - цвета и временные интервалы

---

Наслаждайтесь вашим ретро CRT глитч-сайтом! 📺✨
